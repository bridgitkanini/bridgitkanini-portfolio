{
  "buildCommand": "npm install -g pnpm@10.14.0 && pnpm install && pnpm run build",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
