{
  "buildCommand": "npm install -g pnpm@10.14.0 && pnpm install --no-frozen-lockfile && pnpm run build",
  "installCommand": "npm install -g pnpm@10.14.0 && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
